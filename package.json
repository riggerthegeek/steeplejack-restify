{
  "name": "steeplejack-restify",
  "version": "1.0.0-7",
  "description": "Restify implementation for Steeplejack",
  "main": "./index",
  "scripts": {
    "build": "npm run clean && tsc --declaration --sourceMap typings/main.d.ts index.ts interfaces/*.ts",
    "buildAll": "npm run clean && tsc --outDir ./tmp --sourceMap index.ts interfaces/*.ts test/*.ts",
    "ci": "npm test && npm run coverage",
    "clean": "rm -Rf index.js interfaces/*.js index.js.map interfaces/*.js.map index.d.ts interfaces/*.d.ts tmp coverage",
    "coverage": "npm run buildAll && rm -Rf ./coverage && istanbul cover _mocha ./tmp/test/ -- --recursive ./tmp/test && remap-istanbul -i ./coverage/coverage.json -o ./coverage/lcov-report -t html&& remap-istanbul -i ./coverage/coverage.json -o ./coverage/coverage.json  && istanbul check-coverage ./coverage/coverage.json --statements=100 --functions=100 --branches=100 --lines=100",
    "lint": "tslint *.ts interfaces/*.ts",
    "mocha": "npm run buildAll && mocha --recursive tmp/test",
    "postversion": "git push && git push --tags",
    "test": "npm run lint && npm run mocha",
    "typings": "typings install",
    "watch:coverage": "onchange 'src/**/*' 'test/**/*' -- npm run coverage",
    "watch:test": "onchange 'src/**/*' 'test/**/*' -- npm test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/riggerthegeek/steeplejack-restify.git"
  },
  "keywords": [
    "steeplejack",
    "steeplejack-plugin",
    "restify",
    "server-strategy"
  ],
  "author": "Simon Emms <simon@simonemms.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/riggerthegeek/steeplejack-restify/issues"
  },
  "homepage": "https://github.com/riggerthegeek/steeplejack-restify#readme",
  "dependencies": {
    "es6-promise": "^3.1.2",
    "lodash": "^4.6.1"
  },
  "peerDependencies": {
    "restify": "*",
    "steeplejack": "^2.0.0-9"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "istanbul": "^0.4.2",
    "mocha": "^2.4.5",
    "onchange": "^2.1.2",
    "proxyquire": "^1.7.4",
    "remap-istanbul": "^0.5.1",
    "restify": "^4.0.0",
    "sinon": "^1.17.3",
    "sinon-chai": "^2.8.0",
    "steeplejack": "^2.0.0-9",
    "tslint": "^3.6.0",
    "typescript": "^1.8.9",
    "typings": "^0.7.9"
  }
}
